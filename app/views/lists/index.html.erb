<div class="list-wrap">
<% @lists.each do |list| %>
  <div class="list-box">
    <% if list.image.attached? %>
      <%= image_tag list.image, size: "200x200" %>
    <% else %>
      <%= image_tag 'no_image', size: "200x200" %>
    <% end %>
    <div class="list-comment">
      <span class="list-body"><%= list.body %></span>
      <span class="list-title"><%= link_to list.title, list_path(list.id) %></span>
    </div>
  </div>
<% end %>
</div>

<!-- 以下削除
<h1>投稿一覧</h1>
<% @lists.each do |list| %>
  <p>タイトル</p>
  <!-- <span><%= list.title %></span> を削除して、以下コードに変更 -->
  <span>
    <%= link_to list.title, list_path(list.id) %>
  </span>
  <p>画像</p>
  <%= image_tag list.image, size: "200x200" %>
-->
<!-- 以下削除
 listに紐づくimageを HTMLのimgタグを生成するimage_tagヘルパーにわたす
  <% if list.image.attached? %>list.imageに対して imageが存在するかメソッドで確認している
    <%= image_tag list.image, size: "200x200" %>
  <% else %>
    <%= image_tag 'no_image', size: "200x200" %>
  <% end %>
-->

<% end %>